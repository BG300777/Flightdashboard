<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flight Status Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            color: #333;
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.9);
            padding: 15px 25px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
            margin-bottom: 25px;
        }
        
        .logo {
            display: flex;
            align-items: center;
        }
        
        .logo-icon {
            font-size: 28px;
            margin-right: 10px;
            color: #0066cc;
        }
        
        h1 {
            color: #0066cc;
            font-size: 28px;
        }
        
        .current-time {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            background: #f0f8ff;
            padding: 8px 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        
        .dashboard {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th {
            background: #0066cc;
            color: white;
            padding: 15px;
            text-align: left;
            font-weight: 600;
        }
        
        td {
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
        }
        
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        
        tr:hover {
            background-color: #e8f4ff;
        }
        
        .status {
            display: inline-flex;
            align-items: center;
            padding: 6px 12px;
            border-radius: 20px;
            font-weight: 500;
        }
        
        .status.departed {
            background-color: #e6f7ee;
            color: #0c6b3f;
        }
        
        .status.boarding {
            background-color: #fff8e6;
            color: #d69e02;
        }
        
        .status.not-open {
            background-color: #f2f2f2;
            color: #666;
        }
        
        .status-icon {
            margin-right: 5px;
            font-size: 16px;
        }
        
        .controls {
            display: flex;
            justify-content: space-between;
            padding: 20px;
            background: #f8f9fa;
            border-top: 1px solid #e0e0e0;
        }
        
        .refresh-btn {
            background: #0066cc;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
            display: flex;
            align-items: center;
            transition: background 0.3s;
        }
        
        .refresh-btn:hover {
            background: #0052a3;
        }
        
        .refresh-icon {
            margin-right: 8px;
        }
        
        .auto-refresh {
            display: flex;
            align-items: center;
        }
        
        .auto-refresh label {
            margin-right: 10px;
            font-weight: 500;
        }
        
        .last-updated {
            color: #666;
            font-style: italic;
        }
        
        .instructions {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 20px;
            margin-top: 25px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
        }
        
        .instructions h2 {
            color: #0066cc;
            margin-bottom: 15px;
        }
        
        .instructions ol {
            margin-left: 20px;
            line-height: 1.6;
        }
        
        .instructions li {
            margin-bottom: 10px;
        }
        
        @media (max-width: 768px) {
            th, td {
                padding: 10px;
            }
            
            header {
                flex-direction: column;
                text-align: center;
            }
            
            .current-time {
                margin-top: 10px;
            }
            
            .controls {
                flex-direction: column;
                gap: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <div class="logo-icon">✈</div>
                <h1>Flight Status Dashboard</h1>
            </div>
            <div class="current-time" id="current-time">24 August 2025 • 14:04:00</div>
        </header>
        
        <div class="dashboard">
            <table>
                <thead>
                    <tr>
                        <th>Gate No</th>
                        <th>Schedule</th>
                        <th>Status</th>
                        <th>Last Update</th>
                        <th>Terminal</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>3</td>
                        <td>04:15:00</td>
                        <td><span class="status departed"><span class="status-icon">✅</span> Departed</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>2</td>
                        <td>05:15:00</td>
                        <td><span class="status boarding"><span class="status-icon">💬</span> Boarding</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>7</td>
                        <td>05:25:00</td>
                        <td><span class="status boarding"><span class="status-icon">💬</span> Boarding</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>2A</td>
                        <td>06:10:00</td>
                        <td><span class="status not-open"><span class="status-icon">✅</span> Not Open</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>6</td>
                        <td>07:00:00</td>
                        <td><span class="status not-open"><span class="status-icon">✅</span> Not Open</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>1</td>
                        <td>07:55:00</td>
                        <td><span class="status not-open"><span class="status-icon">✅</span> Not Open</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                    <tr>
                        <td>8</td>
                        <td>08:05:00</td>
                        <td><span class="status not-open"><span class="status-icon">✅</span> Not Open</span></td>
                        <td>24 Aug 2025 • 14:02:48</td>
                        <td>Ter</td>
                    </tr>
                </tbody>
            </table>
            
            <div class="controls">
                <button class="refresh-btn" id="refresh-btn">
                    <span class="refresh-icon">🔄</span> Refresh Status
                </button>
                <div class="auto-refresh">
                    <label for="auto-refresh-check">Auto-refresh every minute:</label>
                    <input type="checkbox" id="auto-refresh-check" checked>
                </div>
                <div class="last-updated" id="last-updated">Last updated: 24 Aug 2025 • 14:04:00</div>
            </div>
        </div>

        <div class="instructions">
            <h2>How to Use This Dashboard</h2>
            <ol>
                <li>Copy all the code from this page</li>
                <li>Open a text editor (Notepad, TextEdit, VS Code, etc.)</li>
                <li>Paste the code into a new file</li>
                <li>Save the file with a .html extension (e.g., "flight-dashboard.html")</li>
                <li>Double-click the saved file to open it in your web browser</li>
                <li>Use the "Refresh Status" button to manually update flight statuses</li>
                <li>Toggle the "Auto-refresh" checkbox to enable/disable automatic updates every minute</li>
            </ol>
            <p><strong>Note:</strong> The status updates are simulated for demonstration purposes. In a real application, these would connect to actual flight data APIs.</p>
        </div>
    </div>

    <script>
        // Update current time
        function updateCurrentTime() {
            const now = new Date();
            const options = { 
                day: '2-digit', 
                month: 'short', 
                year: 'numeric', 
                hour: '2-digit', 
                minute: '2-digit', 
                second: '2-digit',
                hour12: false 
            };
            const formattedTime = now.toLocaleDateString('en-GB', options).replace(',', ' •');
            document.getElementById('current-time').textContent = formattedTime;
        }
        
        // Update flight statuses randomly to simulate real-time changes
        function updateFlightStatuses() {
            const statusCells = document.querySelectorAll('.status');
            const timeCells = document.querySelectorAll('tbody tr td:nth-child(4)');
            const now = new Date();
            
            statusCells.forEach((cell, index) => {
                // Only update some statuses randomly (not all at once)
                if (Math.random() > 0.7) {
                    const statusTypes = ['departed', 'boarding', 'not-open'];
                    const statusIcons = {
                        'departed': '✅',
                        'boarding': '💬',
                        'not-open': '✅'
                    };
                    
                    // Remove all status classes
                    cell.classList.remove('departed', 'boarding', 'not-open');
                    
                    // Select a random status
                    const randomStatus = statusTypes[Math.floor(Math.random() * statusTypes.length)];
                    
                    // Apply new status
                    cell.classList.add(randomStatus);
                    cell.innerHTML = `<span class="status-icon">${statusIcons[randomStatus]}</span> ${randomStatus.charAt(0).toUpperCase() + randomStatus.slice(1).replace('-', ' ')}`;
                    
                    // Update the time for the changed status
                    const timeOptions = { 
                        day: '2-digit', 
                        month: 'short', 
                        year: 'numeric', 
                        hour: '2-digit', 
                        minute: '2-digit', 
                        second: '2-digit',
                        hour12: false 
                    };
                    timeCells[index].textContent = now.toLocaleDateString('en-GB', timeOptions).replace(',', ' •');
                }
            });
            
            // Update last updated time
            const lastUpdated = now.toLocaleDateString('en-GB', { 
                day: '2-digit', 
                month: 'short', 
                year: 'numeric', 
                hour: '2-digit', 
                minute: '2-digit', 
                second: '2-digit',
                hour12: false 
            }).replace(',', ' •');
            document.getElementById('last-updated').textContent = `Last updated: ${lastUpdated}`;
        }
        
        // Initial setup
        updateCurrentTime();
        
        // Set interval to update time every second
        setInterval(updateCurrentTime, 1000);
        
        // Set interval to update flight statuses every 60 seconds if auto-refresh is enabled
        setInterval(() => {
            if (document.getElementById('auto-refresh-check').checked) {
                updateFlightStatuses();
            }
        }, 60000);
        
        // Add event listener for manual refresh button
        document.getElementById('refresh-btn').addEventListener('click', updateFlightStatuses);
        
        // Initial status update
        setTimeout(updateFlightStatuses, 2000);
    </script>
</body>
</html>